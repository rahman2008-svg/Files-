workflows:
  android-release:
    name: Files APK Build

    max_build_duration: 60

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew clean
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
